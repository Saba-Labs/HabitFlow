# Optional: Neon Postgres connection string (app works without it using in-memory auth)
# DATABASE_URL=postgresql://user:pass@host/db?sslmode=require

# Required in production for auth tokens
JWT_SECRET=change-me-to-a-long-random-string

# Optional dev overrides
# PING_MESSAGE=pong
